/**
 * Re-exports from @/theme/colors.
 * Import from @/theme directly in new code.
 * This file exists so the path documented in CLAUDE.md stays valid.
 */
export { colors } from '@/theme/colors';
export type { ColorToken } from '@/theme/colors';
